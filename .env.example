# Copy to .env and fill in. The SEC requires a User-Agent identifying the
# requester (https://www.sec.gov/os/accessing-edgar-data). Never commit .env.
SEC_USER_AGENT="Your Name your.email@example.com"
